#pragma once

#include "Common.h"
#include "DeckLinkAPI_h.h"

const char* BmdColorspaceToStr(BMDColorspace t);
const char* DxgiFormatToStr(DXGI_FORMAT t);
const char* DxgiColorSpaceToStr(DXGI_COLOR_SPACE_TYPE t);
const char* DxgiModeRotationToStr(DXGI_MODE_ROTATION t);








